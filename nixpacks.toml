[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt --no-cache-dir"]

[phases.build]
cmds = []

[start]
cmd = "python3 bot.py"

[caches]
pip = "/root/.cache/pip"
